class AddCartsToRedis < ActiveRecord::Migration
  def change
    #Cart.includes(:user).find_each do |cart|
      #cart.user.cart_hash.clear
      #cart.user.cart_hash.store(cart.ProductID, cart.Number)
    #end
  end
end
